# Railway Configuration - Deploy from backend directory
# This file should be in the root directory to tell Railway to deploy from backend/

[build]
builder = "Nixpacks"

[deploy]
startCommand = "cd backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.env]
PYTHON_VERSION = "3.11"

[deploy.env]
DJANGO_SETTINGS_MODULE = "backend.settings"
SECRET_KEY = "@secret_key"
DEBUG = "False"
ALLOWED_HOSTS = "@railway_static_url"
CORS_ALLOW_ALL_ORIGINS = "False"
CORS_ALLOWED_ORIGINS = "@vercel_url"
GEMINI_API_KEY = "@gemini_api_key"